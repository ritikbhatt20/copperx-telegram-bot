export const apiClient = {
  // Example structure for future use
  async getProfile(token: string) {
    // Will use axios to call /api/auth/me
    console.log("API call to get profile (WIP)");
  },
};
